# Write a Python program to calculate the area of a triangle.

base = float(input("Enter the base : "))
height = float(input("Enter the height : "))

print(f"Area of this triangle is {0.5 * base * height}")
